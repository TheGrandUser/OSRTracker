<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="OSRTracker.Views.Pages.Main.WelcomeStateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OSRTracker.Views.Pages.Main"
    xmlns:vm="using:OSRTracker.ViewModels"
    xmlns:vm2="using:OSRTracker.ViewModels.Pages.Main"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  x:Name="rootPage"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False, Type=vm2:WelcomeStateViewModel}"
    mc:Ignorable="d">

  <UserControl.Resources>
    <DataTemplate x:DataType="vm2:RecentFile" x:Key="recentFile">
      <Border BorderBrush="Black" BorderThickness="2" Background="#EEE" Padding="4">
        <Grid Width="150" Height="120">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>

          <TextBlock Grid.Row="0" Text="{Binding Path=CampaignName}" Style="{StaticResource ListTitleStyle}" />
          <TextBlock Grid.Row="1">System: <Run Text="{Binding Path=SystemName}" /></TextBlock>
          <TextBlock Grid.Row="3" Text="{Binding Path=LastAccessed}" />

          <Button Grid.Row="2" VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Command="{Binding ElementName=rootPage, Path=ViewModel.OpenRecentCampaignCommand}" CommandParameter="{Binding}">
            Open
          </Button>
        </Grid>
      </Border>
    </DataTemplate>
  </UserControl.Resources>

  <Grid>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <CommandBar Background="Transparent" HorizontalAlignment="Left">
        <AppBarButton Icon="Document" Label="New" Command="{Binding Path=CreateCampaignCommand}" />
        <AppBarButton Icon="OpenFile" Label="Open" Command="{Binding Path=OpenCampaignCommand}" />

        <AppBarButton Icon="Clock" Label="Tet Busy" Command="{Binding Path=TestBusyCommand}" />
      </CommandBar>

      <Grid Grid.Row="1">

        <GridView Grid.Row="1" ItemsSource="{x:Bind Path=ViewModel.CampaignsView}"
                  SelectionMode="None"
                  ItemTemplate="{StaticResource recentFile}">

        </GridView>
      </Grid>

    </Grid>

  </Grid>
</UserControl>
