<?xml version="1.0" encoding="utf-8"?>
<Page
  x:Class="OSRTracker.Views.Pages.CharacterRosterPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:OSRTracker.Views.Pages"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:controls="using:CommunityToolkit.WinUI.Controls"
  xmlns:f="using:Windows.Globalization.NumberFormatting"
  xmlns:tv="using:WinUI.TableView"
  xmlns:vm="using:OSRTracker.ViewModels.Pages.CharacterRoster"
  
  d:DataContext="{d:DesignInstance Type=vm:CharacterRosterViewModel, IsDesignTimeCreatable=False}"
  mc:Ignorable="d">

  <Page.Resources>
    <f:DecimalFormatter x:Key="DeciamlFormatter" SignificantDigits="2" />
  </Page.Resources>
  
  <Grid>

    <NumberBox />

    <tv:TableView x:Name="rosterTable" ItemsSource="{x:Bind ViewModel.Characters}" AutoGenerateColumns="False">
      <tv:TableView.Columns>
        <tv:TableViewTextColumn Header="Name" Binding="{Binding Path=Name}" Width="120" />
        <tv:TableViewTextColumn Header="Player Name" Binding="{Binding Path=PlayerName}" Width="120" />

        <tv:TableViewComboBoxColumn Header="Type" Binding="{Binding Path=CharacterType}" ItemsSource="{x:Bind  vm:CharacterTypeViewModel.CharacterTypes}" DisplayMemberPath="Name" />
        <tv:TableViewComboBoxColumn Header="Status" Binding="{Binding Path=CharacterStatus}" ItemsSource="{x:Bind  vm:CharacterStatusViewModel.CharacterStatuses}" DisplayMemberPath="Name" />
        
        <tv:TableViewComboBoxColumn Header="Class" Width="200" Binding="{Binding Path=Class}" ItemsSource="{x:Bind ViewModel.AvailableClasses}" DisplayMemberPath="Name" />

        <tv:TableViewNumberColumn Header="Level" Binding="{Binding Path=Level}" />
        <tv:TableViewNumberColumn Header="XP" Binding="{Binding Path=CurrentXP}" Width="80" />

        <tv:TableViewNumberColumn Header="XP Share" Binding="{Binding Path=ShareMultiplierXP}" />
        <tv:TableViewNumberColumn Header="Treasure Share" Binding="{Binding Path=ShareMultiplierTreasure}" />

        <tv:TableViewNumberColumn Header="{x:Bind ViewModel.StrLabel}" Binding="{Binding Path=Str}" />
        <tv:TableViewNumberColumn Header="{x:Bind ViewModel.IntLabel}" Binding="{Binding Path=Int}" />
        <tv:TableViewNumberColumn Header="{x:Bind ViewModel.WisLabel}" Binding="{Binding Path=Wis}" />
        <tv:TableViewNumberColumn Header="{x:Bind ViewModel.DexLabel}" Binding="{Binding Path=Dex}" />
        <tv:TableViewNumberColumn Header="{x:Bind ViewModel.ConLabel}" Binding="{Binding Path=Con}" />
        <tv:TableViewNumberColumn Header="{x:Bind ViewModel.ChaLabel}" Binding="{Binding Path=Cha}" />
        
      </tv:TableView.Columns>
    </tv:TableView>
    
  </Grid>
</Page>
