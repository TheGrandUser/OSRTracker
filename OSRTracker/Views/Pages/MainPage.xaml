<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OSRTracker.Views.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OSRTracker.Views"
    xmlns:vm="using:OSRTracker.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
  x:Name="rootPage"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False,Type=vm:MainViewModel}">

  <Page.Resources>
    <DataTemplate x:DataType="vm:RecentFile" x:Key="recentFile">
      <!--<Button Command="{x:Bind ViewModel.OpenRecentCampaignCommand}" CommandParameter="{Binding}">
        <TextBlock Text="{Binding Path=CampaignName}" />
      </Button>-->
      <Button Command="{Binding ElementName=rootPage, Path=ViewModel.OpenRecentCampaignCommand}" CommandParameter="{x:Bind}">
        <TextBlock Text="{x:Bind CampaignName}" />
      </Button>
    </DataTemplate>
  </Page.Resources>

  <Grid>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <CommandBar Background="Transparent" HorizontalAlignment="Left">
        <AppBarButton Icon="Document" Label="New" Command="{x:Bind ViewModel.CreateCampaignCommand}" />
        <AppBarButton Icon="OpenFile" Label="Open" Command="{x:Bind ViewModel.OpenCampaignCommand}" />
        <!--<AppBarButton Icon="Undo"  Label="Rollback" Command="{x:Bind ViewModel.RollbackCommand}" />-->
        <AppBarButton Icon="Clock" Label="Tet Busy" Command="{x:Bind ViewModel.TestBusyCommand}" />
      </CommandBar>

      <Grid Grid.Row="1">

        <GridView Grid.Row="1" ItemsSource="{x:Bind ViewModel.CampaignsView}"
                ItemTemplate="{StaticResource recentFile}">

        </GridView>
      </Grid>

      <!--<Border
        Grid.Row="1"
        Height="64" Width="64"
        BorderBrush="Cyan"
        BorderThickness="3"
        HorizontalAlignment="Center" VerticalAlignment="Center">-->


      <!--</Border>-->

    </Grid>


    <!--<Grid Grid.Row="1" Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource ResourceKey=boolToVis}}"
          Background="#44A9A9A9">

      <ProgressRing 
              IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
              Grid.Row="1"
              Background="LightGreen"
              IsIndeterminate="True"
              Height="60" Width="60"
              HorizontalAlignment="Center" VerticalAlignment="Center">

      </ProgressRing>
    </Grid>-->


  </Grid>
</Page>
