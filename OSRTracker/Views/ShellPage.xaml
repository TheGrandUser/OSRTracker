<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OSRTracker.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OSRTracker.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:OSRTracker.Helpers"
    xmlns:behaviors="using:OSRTracker.Behaviors"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    mc:Ignorable="d"
    Loaded="OnLoaded">

  <Grid>
    <Grid x:Name="AppTitleBar"
          Canvas.ZIndex="1"
          Height="{Binding ElementName=NavigationViewControl, Path=CompactPaneLength}"
          IsHitTestVisible="True"
          VerticalAlignment="Top">
      <Image Source="/Assets/WindowIcon.ico"
               HorizontalAlignment="Left"
               Width="16"
               Height="16" />
      <TextBlock x:Name="AppTitleBarText"
                   VerticalAlignment="Center"
                   TextWrapping="NoWrap"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Margin="28,0,0,0"/>
    </Grid>
    <NavigationView
        x:Name="NavigationViewControl"
        Canvas.ZIndex="0"
        IsBackButtonVisible="Visible"
        IsBackEnabled="{x:Bind ViewModel.IsBackEnabled, Mode=OneWay}"
        SelectedItem="{x:Bind ViewModel.Selected, Mode=OneWay}"
        IsSettingsVisible="False"
        ExpandedModeThresholdWidth="1280"
        DisplayModeChanged="NavigationViewControl_DisplayModeChanged"
        Header="{x:Bind ((ContentControl)ViewModel.Selected).Content, Mode=OneWay}">
      <NavigationView.MenuItems>
        <!--
            TODO: Update item titles by updating <x:Uid>.Content entries in Strings/en-us/Resources.resw.
            https://docs.microsoft.com/windows/uwp/app-resources/localize-strings-ui-manifest#refer-to-a-string-resource-identifier-from-xaml
        
            TODO: Update item icons by updating FontIcon.Glyph properties.
            https://docs.microsoft.com/windows/apps/design/style/segoe-fluent-icons-font#icon-list
            -->
        <NavigationViewItem x:Uid="Shell_Main" helpers:NavigationHelper.NavigateTo="OSRTracker.ViewModels.Pages.Main.MainPageViewModel">
          <NavigationViewItem.Icon>
            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xe7c3;"/>
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        
        <NavigationViewItem x:Uid="Shell_SystemEditor" helpers:NavigationHelper.NavigateTo="OSRTracker.ViewModels.Pages.SystemEditor.SystemEditorPageViewModel"
                            IsEnabled="{x:Bind ViewModel.IsProjectOpened, Mode=OneWay}">
          <NavigationViewItem.Icon>
            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xe713;"/>
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        
        <NavigationViewItem x:Uid="Shell_CharacterRoster" helpers:NavigationHelper.NavigateTo="OSRTracker.ViewModels.Pages.CharacterRoster.CharacterRosterPageViewModel"
                            IsEnabled="{x:Bind ViewModel.IsProjectOpened, Mode=OneWay}">
          <NavigationViewItem.Icon>
            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xe716;"/>
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        
        <NavigationViewItem x:Uid="Shell_GamePlay" helpers:NavigationHelper.NavigateTo="OSRTracker.ViewModels.Pages.GamePlay.GamePlayPageViewModel"
                            IsEnabled="{x:Bind ViewModel.IsProjectOpened, Mode=OneWay}">
          <NavigationViewItem.Icon>
            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xe902;"/>
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        
      </NavigationView.MenuItems>
      
      <NavigationView.HeaderTemplate>
        <DataTemplate>
          <Grid>
            <TextBlock
                        Text="{Binding}"
                        Style="{ThemeResource TitleTextBlockStyle}" />
          </Grid>
        </DataTemplate>
      </NavigationView.HeaderTemplate>
      
      <i:Interaction.Behaviors>
        <behaviors:NavigationViewHeaderBehavior
                DefaultHeader="{x:Bind ((ContentControl)ViewModel.Selected).Content, Mode=OneWay}">
          <behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
            <DataTemplate>
              <Grid>
                <TextBlock
                                Text="{Binding}"
                                Style="{ThemeResource TitleTextBlockStyle}" />
              </Grid>
            </DataTemplate>
          </behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
        </behaviors:NavigationViewHeaderBehavior>
      </i:Interaction.Behaviors>
      
      <Grid Margin="{StaticResource NavigationViewPageContentMargin}">
        <Frame x:Name="NavigationFrame" />
      </Grid>
      
    </NavigationView>


    <Grid Grid.Row="0" Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource ResourceKey=boolToVis}}"
          Canvas.ZIndex="2"
          Background="#44A9A9A9">

      <ProgressRing 
          IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
          Grid.Row="1"
          Background="LightGreen"
          IsIndeterminate="True"
          Height="60" Width="60"
          HorizontalAlignment="Center" VerticalAlignment="Center">

      </ProgressRing>
    </Grid>


  </Grid>
</Page>
