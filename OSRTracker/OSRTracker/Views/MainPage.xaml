<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OSRTracker.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OSRTracker.Views"
  xmlns:vm="using:OSRTracker.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
  d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False,Type=vm:MainViewModel}">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>


    <StackPanel Orientation="Horizontal">
      <Button Command="{x:Bind ViewModel.CreateCampaignCommand}">Create</Button>
      <!--<Button Command="{Binding Path=CreateCampaignCommand}">Create</Button>-->
      <Button Command="{x:Bind ViewModel.OpenCampaignCommand}">Open</Button>
    </StackPanel>
    
  </Grid>
</Page>
